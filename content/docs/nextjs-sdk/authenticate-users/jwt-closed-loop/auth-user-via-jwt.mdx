---
title: JWT Authentication
description: Authenticate users via JWT using Okto's NextJS SDK with loginUsingJWTAuthentication method for secure, token-based session management.
full: false
---

import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Callout } from "fumadocs-ui/components/callout";
import { Accordion, Accordions } from "fumadocs-ui/components/accordion";
import "../../../styles.css";
import AIMarkdownComponent from "components/AIMarkdownComponent";

<AIMarkdownComponent isIndex={false} />

The `loginUsingJWTAuthentication()` method authenticates users using JWT tokens. It verifies the JWT token, creates an authentication payload, and sends it to the  Okto chain. Upon successful authentication, it updates the user session.

### Example

<Accordions defaultValue="Usage">
<Accordion title="Usage">
<Tabs items={['TypeScript']}>
<Tab value="TypeScript">
```typescript
import { useOkto } from '@okto_web3/react-sdk';

function JwtAuthentication() {
const oktoClient = useOkto(); // [!code highlight]

    async function handleJwtLogin(jwtToken: string) {
        try {
            const user = await oktoClient.loginUsingJWTAuthentication(jwtToken); // [!code highlight]
            console.log("Login successful:", user);
        } catch (error) {
            console.error("Login failed:", error);
        }
    }

    return (
        <div>
            {/* Your JWT token generation/retrieval UI here */}
            <button onClick={() => handleJwtLogin("your-jwt-token")}>
                Login with JWT
            </button>
        </div>
    );

}

````
</Tab>
</Tabs>
</Accordion>

<Accordion title="Success Response">
```typescript
0x9A94366DC5a9447C763aa2c3A09EB56dc82CB4D3
````

</Accordion>
</Accordions>

<Callout title="Note" type="info">
  For error handling: - Use the error code to debug issues. Check out the [SDK
  errors and warnings](/docs/nextjs-sdk/sdk-error-warnings) documentation - For
  help, navigate to the [troubleshooting guide](/docs/nextjs-sdk/troubleshooting)
  to contact support
</Callout>

### Method Overview

| Method                                                           | Description                        |
| ---------------------------------------------------------------- | ---------------------------------- |
| <sub><i>async</i></sub> `OktoClient.loginUsingJWTAuthentication` | Authenticates user using JWT token |

<div className="method-box">

## Login Using JWT

<sub>
  <i>async</i>
</sub>
`OktoClient.loginUsingJWTAuthentication(jwtToken: string)` authenticates users
using JWT tokens and establishes their session.

### Parameters

| Parameter  | Type     | Description                      | Required |
| ---------- | -------- | -------------------------------- | -------- |
| `jwtToken` | `string` | The JWT token for authentication | Yes      |

### Response

<Callout title="Success Response">

| Field Name | Type               | Description                                                 |
| ---------- | ------------------ | ----------------------------------------------------------- |
| `result`   | `Promise<Address>` | Returns the user's Smart Wallet Address on successful login |

</Callout>

</div>

<Callout title="Important" type="warn">
Before using JWT authentication, ensure you have: 
1. Set up JWT verification with Okto 
2. Created a verification endpoint that Okto can call 
3. Generated a valid JWT token with the required user identification
</Callout>
