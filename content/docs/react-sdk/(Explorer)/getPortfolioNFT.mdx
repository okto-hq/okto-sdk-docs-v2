---
title: Get Portfolio NFTs
description: Learn how to retrieve user's NFT portfolio using the Okto SDK.
full: false
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';
import { Icon as IIcon } from '@iconify/react';

The `getPortfolioNFT` function retrieves the list of NFTs in the authenticated user's portfolio.

### Method Overview

| Method | Description |
|---------|-------------|
| <sub><i>async</i></sub> [`getPortfolioNFT`](#get-portfolio-nft) | Get user's NFT portfolio |

<div className="method-box">

## Get Portfolio NFT

<sub><i>async</i></sub> `getPortfolioNFT(oktoClient: OktoClient)` retrieves the user's NFT balances.

### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `oktoClient` | `OktoClient` | Instance of OktoClient obtained from useOkto hook |

### Response

<Callout title="Success Response">

| Field Name | Type | Description |
|------------|------|-------------|
| `nfts` | `Promise<UserNFTBalance[]>` | Array of user's NFT balances |

</Callout>

<Accordions>
<Accordion title="Example">
<Tabs items={['TypeScript']}>
<Tab value="TypeScript">
```typescript
import { useOkto } from '@okto_web3/react-sdk';
import { getPortfolioNFT } from '@okto_web3/react-sdk';

function NFTPortfolio() {
    const oktoClient = useOkto();

    async function fetchNFTs() {
        try {
            const nfts = await getPortfolioNFT(oktoClient);
            console.log('NFT portfolio:', nfts);
        } catch (error) {
            console.error('Error fetching NFTs:', error);
        }
    }

    return (
        <button onClick={fetchNFTs}>
            Fetch NFT Portfolio
        </button>
    );
}
```
</Tab>
</Tabs>
</Accordion>
</Accordions>

</div>